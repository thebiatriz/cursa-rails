<% content_for :title, t('.title') %>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h1><%= t('.title') %></h1>

  <div>
    <%= link_to t('.back_link'), customers_path, class: "btn btn-outline-secondary" %>
    <%= link_to t('.show_link'), @customer, class: "btn btn-outline-secondary me-2" %>
  </div>
</div>

<%= render "form", customer: @customer %>