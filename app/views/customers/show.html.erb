<p style="color: green"><%= notice %></p>

<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <%= link_to t('.back'), customers_path, class: "btn btn-outline-secondary" %>
  </div>
  <div>
    <%= link_to t('.edit'), edit_customer_path(@customer), class: "btn btn-primary" %>
  </div>
</div>

<%= render @customer %>

<div class="d-flex mt-3 border-top pt-3"> 
  <%= button_to t('.delete'), @customer, method: :delete, class: "btn btn-outline-danger" %>
</div>